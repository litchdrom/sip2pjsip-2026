# Python >= 3.8 required.
#
# sip2pjsip.py  — stdlib only (re, dataclasses, typing, sys), no extra packages needed.
#
# sip2pjsip_realtime.py — requires PyMySQL to connect to MySQL/MariaDB.
pymysql>=1.1.0
